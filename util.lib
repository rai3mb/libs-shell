#!/bin/bash
#By: Raimundo Portela <rai3mb@gmail.com>
# atention: configs in /opt/scripts/scripts.conf
_RS=''
_CONF_FILE=/opt/scripts/scripts.conf
_import() {
	_setVAR 'DIR_LIBS'
	local D=$_RS
	while : ; do
		[ -z "$1" ] && break
		_source "$D/${1}.lib"
		shift
	done
}
_source() { _file "$1" && source "$1"; }
_setVAR() { _getVAR "$1" >/dev/null; [ -z "$_RS" ] && _notFound "$1"; }
_getParam() {
	X="$1"
	while shift; do
		[ "$1" = "$X" ] && {
			[ "$2" ] && echo "$2" && return 0 || return 1
		}
	done
	return 1
}
_getVAR() {
	local V=$(egrep "^$1=" -m1 "$_CONF_FILE" | cut -d'=' -f2-)
	echo "$1" | egrep -q '^DIR_' && {
		[ ! -d "$V" ] && V=''
	}
	_RS="$V" && echo "$V"
}
_dir() { [ -d "$1" ] && return 0 || _notFound "$1"; }
_file() { [ -f "$1" ] && return 0 || _notFound "$1";}
_notFound() { _die "not found : [$1]"; }
_die() { echo -e "\n===[ERROR]===\n${1}\n===[ERROR]===\n"; _killMe; }
_killMe() { kill -15 $$; }
_checkDependences() {
	while : ; do
		[ -z "$1" ] && break
		local V=$(whereis "$1" | cut -d':' -f2-)
		[ ${#V} -eq 0 ] && _notFound "$1"
		shift
	done
}
